import {mkdir,readFile,writeFile} from 'node:fs/promises';
// Dependency-free bundler for this project's local, named ES-module imports.
// Source stays modular; release is one self-contained HTML file.
const modules=['data','terrain','world','construction','progression','invasions','garrison','nightmare','combat','support','game','building-upgrades','art','unit-art','renderer','input','audio','save-codec','save','platform','ui','main'];
let bundle='';for(const name of modules){let source=await readFile(`src/${name}.js`,'utf8');source=source.replace(/^import .*?;\r?\n/gm,'').replace(/^export /gm,'');bundle+=`\n// ${name}\n${source}\n`;}
let html=await readFile('index.html','utf8');const css=(await readFile('style.css','utf8')).replace(/^@import[^;]*;/m,'');html=html.replace('<link rel="stylesheet" href="style.css">',`<style>${css}</style>`).replace('<script type="module" src="src/main.js"></script>',`<script type="module">${bundle.replaceAll('</script','<\\/script')}</script>`);if(process.argv.includes('--crazygames')){html=html.replace('<html lang="en">','<html lang="en" data-platform="crazygames">').replace('<head>','<head>\n  <script src="https://sdk.crazygames.com/crazygames-sdk-v3.js"></script>');}
const output=process.argv.includes('--crazygames')?'dist/crazygames':'dist';await mkdir(output,{recursive:true});await writeFile(output+'/index.html',html);console.log(`Built ${output}/index.html (${Buffer.byteLength(html)} bytes; ${process.argv.includes('--crazygames')?'CrazyGames SDK v3':'no network dependencies'}).`);
